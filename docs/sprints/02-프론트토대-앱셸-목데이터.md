# Sprint 02: 프론트 토대 — 앱 셸 + 라우팅 + 목(mock) 데이터 레이어

> 작성일: 2026-05-30 · 상태: 진행중 (2026-05-30 승인됨)
> 관련 계획서 섹션: 사내CRM_개발계획서_v2.5.md §2(데이터 모델), §5(캘린더·UI), §9(기술 스택)
> 담당: 화면·구동로직 = 작업자(본인) / 서버·인증·배포 = 이팀장(Sprint 01)
> 저장소: https://github.com/ssm741031/crm-20260530.git

## 목표
서버(이팀장 Sprint 01)와 **독립적으로** 화면·로직을 개발할 수 있는 프론트 토대를 만든다.
React + Vite + TypeScript 앱 셸 + 라우팅 + 반응형 레이아웃 + **목 데이터 레이어**까지.
이후 모든 화면(카테고리·할 일·캘린더·고객·파이프라인)이 이 위에 얹힌다.
서버가 완성되면 목 레이어만 진짜 API로 교체한다.

## 왜 이것부터 (추천 근거)
- 서버가 아직 없으므로(이팀장 Sprint 01 승인대기), 목 데이터로 분리하지 않으면 화면 개발이 시작조차 못 한다.
- 앱 셸·라우팅·반응형·디자인 토큰은 **모든 화면의 공통 토대**라 한 번 잘 잡으면 재사용된다.
- 목 데이터 레이어를 "API 계약"과 같은 모양으로 만들면, 나중에 교체가 한 파일 수정으로 끝난다 → 이팀장과의 접점 최소화.

## 완료 기준 (테스트 가능하게)
- [ ] `npm run dev` → 브라우저에서 빈 앱이 뜬다 (PC·모바일 폭 모두 깨지지 않음)
- [ ] 좌측/하단 내비게이션으로 최소 2개 화면(예: 할 일 / 고객) 라우팅 전환된다
- [ ] 목 데이터(고객·할 일·카테고리 샘플)가 화면에 리스트로 출력된다
- [ ] 데이터 호출이 `api/` 한 곳을 통해서만 일어난다 (나중에 진짜 API로 교체할 지점이 한 곳)
- [ ] 색·간격을 디자인 토큰 한 곳에서 바꾸면 전체에 반영된다 (디자인 자유도 확보)
- [ ] `.gitignore`로 `.env`·`node_modules`가 제외되고, 저장소에 첫 커밋이 올라간다

## 영향 받는 파일 (예상)
- `frontend/` — Vite + React + TS 프로젝트 루트
- `frontend/src/app/` — 앱 셸(레이아웃, 내비게이션), 라우팅
- `frontend/src/api/` — **목 데이터 레이어**(나중에 진짜 API로 교체하는 단일 접점)
- `frontend/src/types/` — 계획서 §2 데이터 타입(Task/Customer/Category/Pipeline…) TypeScript 정의
- `frontend/src/mock/` — 샘플 데이터(고객·할 일·카테고리)
- `frontend/src/styles/` — 디자인 토큰(색·간격·반응형 브레이크포인트)
- `.gitignore`, `README` — 저장소 세팅

## 단계별 작업 (step → verify)
1. [ ] 저장소 연결: 이 폴더를 git init → GitHub(crm-20260530) 연결 + `.gitignore` → 검증: `git status` 정상
2. [ ] `frontend/` Vite + React + TS 생성 → 검증: `npm run dev`로 기본 화면 뜸
3. [ ] 디자인 토큰 + 반응형 레이아웃 셸(헤더/내비) → 검증: PC·모바일 폭에서 안 깨짐
4. [ ] 계획서 §2 데이터 타입을 `types/`에 정의 → 검증: 타입 컴파일 통과
5. [ ] 목 데이터 + `api/` 레이어(타입과 동일 모양) → 검증: 샘플 데이터 반환 확인
6. [ ] 라우팅 + 빈 화면 2개(할 일/고객)에서 목 데이터 리스트 출력 → 검증: 화면 전환·리스트 표시
7. [ ] 첫 커밋 + 작업로그 기록(커밋 해시) → 검증: GitHub에 반영

## 리스크 / 주의 (금지사항 체크리스트 연동)
- `.env`·시크릿·고객 개인정보 **커밋 금지** → 1단계에서 `.gitignore` 먼저 (CLAUDE.md 6-B)
- 목 데이터에 **실제 고객 개인정보 넣지 말 것** — 가짜 샘플만 (개인정보 금지사항)
- API 레이어 모양은 이팀장과 합의한 **API 계약**을 따라야 교체가 쉬움 → 계약 미확정 시 §2 데이터 모델을 잠정 기준으로
- 요청 범위 밖 화면 구현은 이 스프린트에서 하지 않음 (토대까지만)

## 자기비판 (이 계획의 약점)
- 이팀장과 **API 계약을 아직 문서로 확정하지 않았다.** §2를 잠정 기준으로 가되, 인증 방식(세션/JWT)·파일 업로드 경유 방식이 정해지면 api 레이어를 한 번 조정해야 할 수 있다.
- 디자인 자유도를 어디까지(테마 전환까지? 색만?) 줄지 미정 — 토큰 구조만 잡고 세부 테마는 화면 작업하며 확장.

---
**선결 과제 (착수 전 확인)**:
- ☐ 이팀장과 API 계약 기준 = 계획서 §2로 잠정 합의해도 되는가
- ☐ git 저장소를 이 폴더(사내crm)에 init 해도 되는가 (문서들도 함께 버전관리됨)

**승인**: ☐ 사용자 승인 받음 (승인 전 구현 금지)
