# 2026-05-30 작업로그

> 이 파일은 백업·복구 원본. 삭제·수정 금지. 그날 작업을 이어서 누적 기록.

---

## [작업 1] 저장소 초기화 + 문서 baseline 커밋 — (Sprint 02 1단계)

- **무엇을**: 이 폴더(`사내crm`)를 git 저장소로 초기화하고 기존 문서를 첫 커밋으로 박제
- **왜** (사용자 요청 추적): "서버는 이팀장, 나는 화면·구동로직 담당, 그것부터 진행" → 화면 개발 토대를 만들기 위한 첫 단계. 복구 좌표(커밋 해시) 확보 목적.
- **어떻게**:
  - `git init` + `main` 브랜치 + 원격 `origin`(crm-20260530.git) 연결
  - `.gitignore` 작성: `node_modules/`·`dist/`·`.env*`·`*.key`·`uploads/`·`attachments/` 등 (개인정보·시크릿·첨부실물·의존성 제외 — CLAUDE.md 6-B)
- **변경 파일 목록**:
  - `.gitignore` (신규)
  - (기존 문서 전체를 추적 시작)
- **이전 상태 → 이후 상태**: git 미추적 폴더 → 문서 baseline 커밋된 저장소
- **커밋 해시**: `fbd757c` (docs baseline = 프론트 작업 전 복구 좌표)
- **검증**: `git status` 정상, `node_modules`·시크릿 스테이징 0개 확인
- **남은 것 / 다음 작업**: 프론트 프로젝트 생성

---

## [작업 2] 프론트 토대 구축 — 앱 셸 + 라우팅 + 목 데이터 (Sprint 02 2~7단계)

- **무엇을**: React+Vite+TypeScript 프로젝트 생성, 반응형 앱 셸, 디자인 토큰, 목 데이터 레이어, 화면 2개(할 일/고객) + 자리표시 3개
- **왜** (사용자 요청 추적): 서버(이팀장 Sprint 01)와 독립적으로 화면·로직을 개발하기 위해 목 데이터 우선 전략 채택(사용자 선택). 기술 스택은 사용자 선택 = React+Vite+TS.
- **어떻게**:
  - `npm create vite@latest frontend -- --template react-ts` + `react-router-dom` 설치
  - `src/styles/tokens.css`: 색·간격·타이포·레이아웃을 CSS 변수로 일괄 관리 (디자인 자유도)
  - `src/app/AppShell.tsx` + `AppShell.css`: PC=좌측 사이드바 / 모바일(≤768px)=하단 탭바, CSS 미디어쿼리로 자동 전환
  - `src/app/nav.ts`: 내비 항목 정의(할 일/고객/캘린더/파이프라인/카테고리)
  - `src/types/index.ts`: 계획서 §2 데이터 모델(Task/Customer/Category/Pipeline/Stage 등) TS 타입 = API 계약
  - `src/mock/data.ts`: 가짜 샘플 데이터(실제 개인정보 아님)
  - `src/api/index.ts`: 데이터 접근 단일 통로(Promise 반환). 서버 완성 시 이 파일만 fetch로 교체
  - `src/pages/`: TasksPage(할 일 목록), CustomersPage(고객+연락처 마스킹), Placeholder(자리표시), page.css
  - `src/utils/mask.ts`: 연락처 부분 마스킹 010-****-5678 (계획서 §8.5)
  - `src/main.tsx`: createBrowserRouter 라우팅 구성
- **변경 파일 목록**:
  - `frontend/` 전체 (위 파일들 + Vite 기본 구성)
  - `.claude/launch.json` (미리보기 서버 설정)
- **이전 상태 → 이후 상태**: 코드 0 → 브라우저에서 뜨는 반응형 앱(목 데이터 표시)
- **커밋 해시**: `0a3c40b` (프론트 토대 = 화면 작업 시작 복구 좌표)
- **검증** (미리보기 서버로 실제 확인):
  - PC: 사이드바·활성메뉴 하이라이트·카드·완료체크(초록)·취소선·태그칩 정상
  - 모바일(375px): 하단 탭바 노출·사이드바 숨김·카드 세로정렬 정상
  - 고객 화면: 라우팅 전환 정상, 연락처 마스킹 `010-****-1234`/`010-****-5678` 작동, 수신동의 표시
  - `npm run build` TS 컴파일 에러 0
- **남은 것 / 다음 작업**: 다음 스프린트에서 실제 화면(카테고리 관리 → 할 일 편집폼 → 캘린더) 구현. 이팀장과 API 계약(§2) 정식 확정.

---

## [작업 3] 트러블슈팅 — index.css 미반영(무스타일) 해결

- **무엇을**: 첫 미리보기에서 데이터는 떴으나 CSS가 전혀 적용 안 되던 문제 해결
- **왜**: OneDrive 폴더에서 Write가 충돌하며 `index.css`가 Vite 기본 템플릿 내용 그대로 남음 → `@import tokens.css` 누락 → 모든 `var(--...)` 변수가 빈 값 → 카드·사이드바 스타일 전부 붕괴
- **어떻게**: `tokens.css`/`AppShell.css`/`page.css`는 정상 확인, `index.css`만 재작성(@import 포함) 후 재검증
- **검증**: 재작성 후 `grep @import` 1개 확인, 페이지 리로드 → 정상 스타일 적용 확인
- **남은 것 / 다음 작업**: 금지사항에 "OneDrive Write 충돌 → 작성 후 즉시 검증" 규칙 추가(아래 반영)

---

## [작업 4] 계층형 카테고리 관리 화면 (Sprint 03)

- **무엇을**: 활동(부모)→세부(자식) 2단 트리 카테고리를 추가·수정·삭제하는 화면
- **왜** (사용자 요청 추적): "1번부터" = 추천 순서 1번(카테고리 관리). 할 일·고객이 참조하는 기준 데이터라 먼저 구축.
- **어떻게**:
  - `api/index.ts`: createCategory/updateCategory/deleteCategory 추가. 메모리 목 상태(`categories`)를 변경. '기본'(c0) 삭제 시 reject. 부모 삭제 시 자식 id도 함께 제거(cascade).
  - `pages/CategoriesPage.tsx`: 부모-자식 트리 렌더, 활동 추가 폼(이름+색상), 세부 추가(부모 색상 상속), 인라인 이름 수정, 삭제(window.confirm + 자식 동반 경고)
  - `pages/CategoriesPage.css`: 트리·버튼·색 점·들여쓰기 스타일
  - `main.tsx`: categories 라우트를 Placeholder → CategoriesPage 교체
- **변경 파일 목록**:
  - `frontend/src/api/index.ts`, `frontend/src/pages/CategoriesPage.tsx`(신규), `frontend/src/pages/CategoriesPage.css`(신규), `frontend/src/main.tsx`
- **이전 상태 → 이후 상태**: 카테고리 = 자리표시 화면 → 실제 CRUD 동작 화면
- **커밋 해시**: `9c3e1ce`
- **검증** (미리보기 DOM 쿼리로 실제 동작 확인):
  - 활동 추가 "다이렉트 업무" → 트리에 표시(5→6개)
  - 세부 추가 "자동차 다이렉트"(부모 아래, 색상 상속) → 6→7개
  - 인라인 수정 "자동차 다이렉트(수정됨)" → 반영
  - 부모 삭제(자식 동반) → "다이렉트 업무"+자식 함께 제거(7→5 복귀)
  - '기본'은 버튼 없음(가드), 자식엔 +세부 없음(2단 제한)
  - `npm run build` 통과, 콘솔 에러 favicon 404뿐(무해)
- **남은 것 / 다음 작업**: 추천 순서 2번 = 할 일 편집폼(마감/구간, 15분 드롭다운, 반복, 알람)

---

## [작업 5] 할 일 편집폼(핵심) + 목록 CRUD (Sprint 04)

- **무엇을**: 할 일을 만들고/고치고/지우는 편집폼과 목록 연동
- **왜** (사용자 요청 추적): "계속해/진행해" = 추천 순서 2번. 캘린더(다음 큰 작업)는 할 일 데이터가 있어야 그릴 수 있어 편집폼이 선행.
- **어떻게**:
  - `api/index.ts`: 할 일 CRUD(createTask/updateTask/deleteTask) + toggleDone, `TaskInput` 타입 export
  - `utils/time.ts`: 15분 단위 시간 옵션 96개(`TIME_OPTIONS`) + 정렬키 헬퍼
  - `components/TaskForm.tsx`: 제목·고객(선택)·카테고리(부모>자식 드롭다운)·마감/구간 토글·날짜+15분 시간·태그(Enter 추가). 반복/알람/공유/첨부는 자리표시. 간단 검증(제목·마감일 필수, 시작>종료 방지).
  - `pages/TasksPage.tsx`: "+새 할 일" 버튼, 카드 클릭→수정, 삭제(확인창), 완료 토글(체크 버튼), 카테고리 색 칩
  - `index.css`: 공통 버튼(.btn) 전역화 / `CategoriesPage.css`: 중복 .btn 제거
- **변경 파일 목록**:
  - `frontend/src/api/index.ts`, `frontend/src/utils/time.ts`(신규), `frontend/src/components/TaskForm.tsx`·`TaskForm.css`(신규), `frontend/src/pages/TasksPage.tsx`, `frontend/src/pages/page.css`, `frontend/src/index.css`, `frontend/src/pages/CategoriesPage.css`
- **이전 상태 → 이후 상태**: 할 일 = 읽기 전용 목록 → 추가/수정/삭제/완료토글 가능
- **커밋 해시**: `caf663f`
- **검증** (미리보기에서 실제 동작):
  - 새로 만들기: "신규 작성 테스트"(장기보험) 추가 → 3→4건
  - 완료 토글: 체크 시 초록+취소선
  - 수정: 카드 클릭 → 값 채워짐 → 제목 변경 저장 반영
  - 삭제: 확인창 → 목록에서 제거
  - 구간형 토글: 시작 날짜/시간 필드 등장(날짜 input 2개), 시간 드롭다운 96개 확인
  - 콘솔 에러 0
- **검증 중 실수(기록)**: preview 상호작용을 reload와 병렬 실행해 목 데이터 항목이 잘못 수정·삭제됨(목이라 새로고침 후 원복, 실제 손실 없음). → 금지사항에 규칙 추가.
- **남은 것 / 다음 작업**: 반복·알람을 폼에 추가(Sprint 05) → 그 다음 캘린더 뷰

---

## [작업 6] 기준 문서 위치 정정 — 루트 → docs/기준/ (작업 검토 후속)

- **무엇을**: `00_금지사항_체크리스트.md`·`01_결정사항_누적.md`를 프로젝트 루트에서 `docs/기준/`로 이동
- **왜** (사용자 요청 추적): "작업한 내용 검토해줘" → 검토 중 발견. CLAUDE.md 0·2·8장은 기준 문서 경로를 `docs/기준/`로 지정했는데 실제 파일은 루트에 있어, 매 세션 0장 필수 절차("`docs/기준/...` 읽기")가 깨질 위험. "1번부터 시작" 지시로 정정.
- **어떻게**: `mkdir docs/기준` 후 `git mv` 2건. 내용 변경 없음(git이 rename 100% 인식). CLAUDE.md는 이미 올바른 경로라 수정 불필요 — 실제 위치를 문서에 맞춤.
- **변경 파일 목록**:
  - `00_금지사항_체크리스트.md` → `docs/기준/00_금지사항_체크리스트.md` (이동)
  - `01_결정사항_누적.md` → `docs/기준/01_결정사항_누적.md` (이동)
- **이전 상태 → 이후 상태**: 기준 문서가 루트(CLAUDE.md 경로와 불일치) → `docs/기준/`(일치)
- **커밋 해시**: `50446f8`
- **검증**: `git status`에 rename(R) 2건·내용 변경 0, 루트에 기준 문서 잔존 0개, 새 위치 첫 줄(`# 금지사항 체크리스트`/`# 결정사항 누적`) 확인. CLAUDE.md 8장 문서구조와 일치.
- **남은 것 / 다음 작업**: ①`README_파일안내서.md` 섹션 2가 기준 문서를 파일명만 표기(경로 없음) — 위치 이동 반영은 사용자 확인 후. ②검토에서 나온 2번(Sprint 04 승인 체크박스 갱신)·3번(`git push`) 처리 대기.

---

## [작업 7] Sprint 04 계획서 승인 체크박스 정정 (검토 후속)

- **무엇을**: `docs/sprints/04-할일편집폼-핵심.md` 하단 승인 체크박스 `☐ → ☑` 갱신
- **왜** (사용자 요청 추적): "작업한 내용 검토해줘" 중 발견 — 구현·커밋(caf663f) 완료됐으나 계획서 승인란이 미체크라 CLAUDE.md 3장(승인 전 구현 금지)과 문서상 모순. "2번 진행해" 지시로 정정.
- **어떻게**: 승인 근거를 함께 명시(2026-05-30 "계속해/진행해" 지시 = 추천 순서 2번 진행 승인, 작업로그 [작업 5] 참조). 사후 정정임을 한 줄로 부기해 감사 추적 정직성 유지(승인 사실 날조 아님).
- **변경 파일 목록**: `docs/sprints/04-할일편집폼-핵심.md`
- **이전 상태 → 이후 상태**: 승인란 미체크(구현과 모순) → ☑ + 승인 근거·정정 경위 명시
- **커밋 해시**: `a4a9d96` (계획서 정정 + 이 로그 동시 커밋)
- **검증**: `grep "☑ 사용자 승인"` 1건 확인.
- **남은 것 / 다음 작업**: 검토 3번(`git push`) 및 README 경로 반영(확인 후) 대기.

---

## [작업 8] README 파일안내서 경로 반영 (검토 후속)

- **무엇을**: `README_파일안내서.md` 섹션 2의 기준 문서 표기에 실제 위치(`docs/기준/`) 반영
- **왜** (사용자 요청 추적): [작업 6]에서 기준 문서를 `docs/기준/`로 이동했으나 README는 파일명만 표기(루트에 있는 듯한 인상). "README 정리부터" 지시로 일치.
- **어떻게**: 섹션 2 표 헤더 `파일`→`파일 (위치)`, 기준 문서 2줄에 `docs/기준/` 경로 명시. 더불어 작업로그/스프린트 템플릿 행에도 실제 작성 위치(`docs/작업로그/`, `docs/sprints/`) 안내 보강.
- **변경 파일 목록**: `README_파일안내서.md`
- **이전 상태 → 이후 상태**: 기준 문서가 파일명만 표기(위치 불명) → `docs/기준/` 경로 명시(실제와 일치)
- **커밋 해시**: `94dc817` (README 정정 + 이 로그 동시 커밋)
- **검증**: `grep "docs/기준/00_금지사항"` 1건 확인.
- **남은 것 / 다음 작업**: 검토 3번(`git push`) — 원격 인증 확인 후. 코드 신규 작업은 Sprint 05(반복·알람).

---

## [작업 9] 편집폼 반복 5종 + 알람(최대5) 추가 (Sprint 05)

- **무엇을**: 할 일 편집폼의 자리표시였던 반복·알람을 실제 입력 UI로 구현
- **왜** (사용자 요청 추적): "다음 코드 작업 진행하자"(서버는 이팀장 내일) → 작업로그상 다음 큰 작업 = Sprint 05. plan 작성·승인("진행해") 후 구현. 범위는 사용자 결정(자동재생성 제외, 반복+알람만).
- **어떻게**:
  - `utils/repeat.ts`(신규): REPEAT_OPTIONS(반복5종+안함), WEEKDAYS(0=일~6=토), REMINDER_PRESETS(정시/15/30/60/1440분), REMINDER_ACTIONS(작업완료/연장/알람만), MAX_REMINDERS=5
  - `TaskForm.tsx`: 반복 드롭다운 + "요일 지정" 시 요일칩 토글(다른 반복 전환 시 repeatDays 비움), 알람 행(시점 프리셋+직접입력 분 / 동작 / 삭제) 추가·5개 제한, handleSave에 repeat/repeatDays/reminders 반영 + weekly_days 요일 미선택 검증. addReminder/updateReminder/removeReminder는 함수형 setState(빠른 연속 클릭 stale-closure 누락 방지 — 검증 중 발견해 수정).
  - `TaskForm.css`: 요일칩·알람행·삭제버튼·안내문 스타일
  - 알람은 **설정 저장만**(실제 발송 X), "자동 완료 안 됨" 안내문 표기(계획서 §4.2 작업완료 자동화 금지 원칙)
- **변경 파일 목록**: `frontend/src/utils/repeat.ts`(신규), `frontend/src/components/TaskForm.tsx`, `frontend/src/components/TaskForm.css`, `docs/sprints/05-반복알람-편집폼.md`(신규, 승인·완료 갱신)
- **이전 상태 → 이후 상태**: 반복·알람 = "🔜 다음 단계" 자리표시 → 실제 입력·저장 가능 (공유·첨부만 자리표시 잔존)
- **커밋 해시**: `fbde320`
- **검증** (미리보기, 상호작용 순차):
  - 반복 드롭다운 6종 라벨 정상(반복안함/매일/요일지정/매주/매월/매년)
  - "요일 지정" → 요일칩 7개 노출, 월·수·금 토글 → `--on` 정확 반영
  - 알람 "+추가" 6회(함수형 수정 후) → 5행 + 버튼 비활성 + "알람 (5/5)" 라벨
  - 시점 "직접 입력(분)" → 숫자 필드(기본 45) 노출
  - 저장: "반복알람 저장검증"(매월, 알람5) → 3→4건, 카드에 "반복" 표시
  - 재오픈: 제목·반복(매월)·알람 5행·첫 알람 커스텀 45분 모두 복원(값 유지)
  - 콘솔 에러 0, `tsc --noEmit`/`npm run build` 통과
- **남은 것 / 다음 작업**: Sprint 06 = 자동 재생성(완료 트리거·안내목표·리드타임, Task 타입 확장) → 이후 캘린더 뷰·습관 뷰. 서버 연결(이팀장, 2026-05-31)되면 api/만 fetch 교체.

---

## [작업 10] 완료 시 자동 재생성 + 리드타임 분리 (Sprint 06)

- **무엇을**: 반복(매주/매월/매년)+자동재생성 켠 할 일을 완료하면 다음 회차가 만기일 역산으로 자동 생성
- **왜** (사용자 요청 추적): "자동재생성 진행해줘" → Sprint 05 다음 작업. plan 작성·승인("진행해") 후 구현. 범위는 사용자 결정("폼 입력+완료시 즉시 재생성", 스케줄러는 서버=이팀장). 도중 "서버는 이팀장, 오늘은 화면만" 재확인 — plan이 이미 스케줄러 제외라 부합.
- **어떻게**:
  - `types`: Task에 `regenNoticeDaysBefore`·`regenLeadDays` 추가(만기일=기존 endDate 겸용). `mock/data` 3건에 기본값 null
  - `utils/regen.ts`(신규): `addPeriod`(주/월/년+말일·윤년 보정), `computeRegenDates`(생성·안내·만기 역산), `canAutoRegen`(매주/매월/매년만), NOTICE/LEAD 프리셋
  - `TaskForm`: 주기형일 때만 autoRegen 토글 노출, 안내목표(30/60/90+직접)·리드타임(1/3/7/14+직접) 입력, 3단 날짜 미리보기. handleSave에 반영(주기형 아니면 강제 off)
  - `api.toggleDone`: '완료로 전환' + autoRegen + 주기형이면 다음 만기(endDate+1주기)로 1건 생성, streak+1. 완료 **해제** 시엔 무생성(무한루프 방지)
  - 실제 '리드타임 전 미리 생성' 스케줄러는 서버 몫임을 폼 안내문·주석에 명시
- **변경 파일 목록**: `frontend/src/types/index.ts`, `frontend/src/mock/data.ts`, `frontend/src/utils/regen.ts`(신규), `frontend/src/components/TaskForm.tsx`·`TaskForm.css`, `frontend/src/api/index.ts`, `docs/sprints/06-자동재생성-리드타임.md`(신규)
- **이전 상태 → 이후 상태**: autoRegen = 미사용 boolean → 폼 설정+완료 트리거 재생성 동작
- **커밋 해시**: `cb9db40` (※ 커밋 직후 git mmap 경고 떴으나 커밋 객체는 정상 기록됨, 트리 clean 확인)
- **검증** (미리보기, 상호작용 순차):
  - 토글 노출: 매월=표시 / daily=숨김 / 매월 복원=표시
  - 3단 미리보기: 만기 8/1·안내60·리드7 → 생성 2026-05-26·안내 2026-06-02·만기 2026-08-01 (계획서 예시 일치)
  - 완료: autoRegen 매월(만기 8/1) 완료 → 4→5건, 새 회차 만기 2026-09-01·미완료·🔥1(streak+1), 원본은 완료 유지
  - 완료 해제: 재생성 안 함(5건 유지) — 무한루프 없음
  - 값 유지: 새 회차 재오픈 시 autoRegen 켜짐·안내60·리드7 복원, 미리보기 만기 9/1 기준 재계산
  - 날짜 단위검증(node): 말일보정(1/31+1월→2/28), 윤년(2/29+1년→2/28) 정확
  - 콘솔 에러 0, tsc/build 통과
- **남은 것 / 다음 작업**: 캘린더 뷰(일·주·월·연) 또는 습관 뷰(streak·주간 그리드). 빼앗긴 고객 처리·재생성 안전장치·스케줄러는 서버(이팀장)와 §2 계약 확정 시.

---

## [작업 11] 캘린더 4종 뷰(읽기전용) + 보기제어 필터 (Sprint 07)

- **무엇을**: 할 일을 일·주·월·연 4종 캘린더 뷰로 보기 + 뷰 전환·날짜 이동·필터·클릭 편집
- **왜** (사용자 요청 추적): "캘린더 뷰부터" → Sprint 06 다음. plan 작성·승인("진행해") 후 구현. 범위는 사용자 결정(4종 읽기전용, 드래그 제외). 서버 연결은 이팀장.
- **어떻게**:
  - `utils/calendar.ts`(신규): monthMatrix(6주)·weekDates·addDays/addMonths·occursOnDate(마감=endDate/구간=시작~종료 범위)·tasksOnDate·applyFilters. 주 시작=일(0), 월 이동 말일 클램프
  - `CalendarPage.tsx`(신규): 뷰 전환(일/주/월/연)+이전/다음/오늘. 월간(그리드·●마감/▭구간·+N 압축·빈칸 더블클릭 새할일·칩 클릭 편집), 주간(7칸·요일헤더→일간), 일간(07~21시 시간대·마감마커/구간막대), 연간(12개월 카드·건수·카테고리 색점·클릭→월간), 보기제어 필터(완료/반복/카테고리) 4뷰 공통
  - `CalendarPage.css`(신규): 그리드·칩·시간대·카드 스타일(반응형 포함)
  - `TaskForm`: `initialDate` prop 추가(캘린더 날짜 더블클릭 시 만기일 프리필)
  - `main.tsx`: /calendar 라우트 Placeholder→CalendarPage
  - 빌드 중 미사용 handleToggle 제거(tsc -b noUnusedLocals) — 캘린더는 클릭=편집만
- **변경 파일 목록**: `frontend/src/utils/calendar.ts`(신규), `frontend/src/pages/CalendarPage.tsx`·`CalendarPage.css`(신규), `frontend/src/components/TaskForm.tsx`, `frontend/src/main.tsx`, `docs/sprints/07-캘린더뷰-읽기전용.md`(신규)
- **이전 상태 → 이후 상태**: /calendar = 자리표시 → 실제 4종 뷰 동작
- **커밋 해시**: `1a9f7d0` (※ git mmap 경고 떴으나 커밋 객체 정상, 트리 clean 확인)
- **검증** (미리보기, 상호작용 순차):
  - 월간: 2026년 5월 42칸, 칩 3개(5/30·5/31·6/1), ●/▭ 구분
  - 연간: 12카드, 5월 2건(색점2)/6월 1건(색점1), 6월 카드 클릭→월간 6월 전환
  - 주간: 05-31~06-06, 요일헤더(일31~토6), 일31·월1에 칩, 헤더 클릭→일간
  - 일간: 2026-05-31, 07~21시 15행, 14:00에 마감 막대
  - 필터: 완료 숨김 3→2, 장기보험 카테고리 끄기 3→2(취소선), 반복 숨김 동작
  - 칩 클릭→편집폼(t2 값), 빈칸(15일) 더블클릭→"새 할 일" 만기일 2026-05-15 프리필
  - 콘솔 에러 0, tsc/build 통과, 월그리드 node 단위검증(첫주 일요일·42칸) 통과
- **남은 것 / 다음 작업**: Sprint 08 = 드래그(날짜/시간 이동, Pointer Event+터치) → 또는 습관 뷰. 검색·30분 겹침 호버펼침·외부캘린더·반복 자동전개는 후속/서버.

---

## [작업 12] 습관 뷰 (반복 streak + 주간 그리드) (Sprint 08)

- **무엇을**: 반복 켜진 할 일(=습관)만 모아 streak·주간 적용 그리드·오늘 체크로 보는 화면
- **왜** (사용자 요청 추적): "계속 진행해줘" → 드래그(외부 라이브러리 필요) 대신 자체완결 가능한 습관 뷰 선택(작업자 제안·진행). plan 작성·승인("진행해") 후 구현. 서버 연결은 이팀장.
- **어떻게**:
  - `utils/habit.ts`(신규): isHabit/habitsOf(repeat≠none), isApplicableOn(daily/weekly_days/weekly/monthly/yearly 적용판정, 기준일=endDate, 일=0), habitCellState(today/future/past/none)
  - `api.toggleHabitDone`(신규): 완료 시 streak+1 / 해제 −1(최소0), doneAt. **공용 toggleDone은 불변**(할 일·캘린더 회귀 방지)
  - `HabitsPage`(신규): 습관 목록(카테고리색·반복종류·🔥streak), 오늘 체크 토글, 주간 그리드(적용=체크/예정·/기록없음?/해당없음—), 더블클릭→편집폼
  - `nav.ts`: "습관"(🔁) 추가, `main.tsx`: /habits 라우트
  - 과거 일자별 달성이력 없음 → '기록 없음(?)' 표기, ✓/✗ 날조 안 함(가짜 금지). 이력은 서버 연동 후
- **변경 파일 목록**: `frontend/src/utils/habit.ts`(신규), `frontend/src/pages/HabitsPage.tsx`·`HabitsPage.css`(신규), `frontend/src/api/index.ts`, `frontend/src/app/nav.ts`, `frontend/src/main.tsx`, `docs/sprints/08-습관뷰.md`(신규)
- **이전 상태 → 이후 상태**: 습관 화면 없음 → 반복 항목 streak·적용 그리드·오늘 체크 동작
- **커밋 해시**: `2652995` (※ git mmap 경고 떴으나 커밋 객체 정상, 트리 clean 확인)
- **검증** (미리보기, 상호작용 순차):
  - 습관 1개(t2 매일·🔥5·오늘✓) 표시, 반복 없는 건 제외
  - 오늘 체크 토글: 5→4(해제 ○) → 5(체크 ✓) streak 증감 정확
  - daily 그리드 7일 적용(과거=기록없음?), 요일지정(월수금)으로 변경 → 월수금만 적용·일화목토="—", 오늘(토)는 월수금 아니라 체크 비활성
  - 더블클릭→편집, 반복 "안 함"으로 저장 → 습관 0개(목록 제외)
  - 모바일(375)에서 그리드 세로 스택 정상, 콘솔 에러 0, tsc/build 통과
  - 적용판정 node 단위검증(daily 전체·월수금만·weekly 토만) 통과
- **남은 것 / 다음 작업**: Sprint 09 = 드래그(날짜/시간 이동, Pointer Event+터치 라이브러리 검토). 통합검색·30분 겹침 호버펼침·일자별 완료이력(서버)·반복 자동전개(서버)는 후속.

---

## 오늘 요약
- 완료: Sprint 02~04 + 기준문서/승인란/README 정정 + Sprint 05(반복·알람) + 06(자동재생성) + 07(캘린더 4종 뷰) + 08(습관 뷰), 커밋 다수(…`1a9f7d0`,`9dd856d`,`2652995`)
- 미완료/이월: GitHub `git push`(원격 인증 필요 — 이팀장과 확인 후), 드래그(Sprint 09), 통합검색·외부캘린더. 서버측(스케줄러·빼앗긴 고객·재생성 안전장치·반복 자동전개·일자별 완료이력)은 이팀장.
- 새로 추가한 결정사항: 기술스택 React+Vite+TS, 목 데이터 우선, api/ 단일 통로 → `01_결정사항_누적.md` 반영
- 새로 추가한 금지사항: ①OneDrive Write 후 즉시 검증 ②preview 상호작용은 reload와 병렬 실행 금지 → `00_금지사항_체크리스트.md` 반영
