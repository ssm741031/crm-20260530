# 2026-06-01 작업로그

> 이 파일은 백업·복구 원본. 삭제·수정 금지. 그날 작업을 이어서 누적 기록.

---

## [작업 1] 활동량·실천 피드백 화면 (Sprint 17)

- **무엇을**: 누적 데이터로 "계획 대비 실천"을 보는 활동량 화면 (계획서 §6)
- **왜** (사용자 요청 추적): "캘린더 진행해" → 현황 확인 결과 캘린더는 완성(외부연동만 서버 의존). 선호 없음 → 추천한 활동량 화면 선택("활동량 화면 추천" → "진행해").
- **어떻게**:
  - `utils/activity.ts`(신규): pct·todayProgress·rangeProgress(7/30일)·byCategory·habitStreaks·dailyDoneTrend 순수 함수. calendar 날짜 헬퍼 재사용. 0 division 가드.
  - `pages/ActivityPage.tsx`(신규): useAuth+canViewTask로 권한 필터 후 집계. 오늘 처리율 원형 게이지(conic-gradient), 7/30일 달성률 카드, 7일 완료 추이 막대, 카테고리 분포 막대, 습관 streak 목록.
  - `ActivityPage.css`(신규): 게이지·카드·막대 스타일
  - `nav.ts`/`main.tsx`: "활동량" + /activity 라우트
- **변경 파일 목록**: `frontend/src/utils/activity.ts`(신규), `frontend/src/pages/ActivityPage.tsx`·`.css`(신규), `frontend/src/app/nav.ts`, `frontend/src/main.tsx`
- **이전 상태 → 이후 상태**: 활동량 화면 없음 → 달성률·분포·streak 집계 화면
- **커밋 해시**: `b22f870` (기능), 문서 커밋 별도
- **검증** (미리보기):
  - 팀원(u3): 권한 범위만 집계(customerId=null·private 항목은 1차 한계로 제외)
  - 대표(u1): 전체 33%(1/3), 기본 1/2(50%), 습관 "매일 아침 DB점검 🔥5일", 오늘 처리율 게이지
  - 권한 차이 확인 = §8 통계 우회 방지 정상. tsc/빌드 통과, 콘솔 에러 0. 세션 u3 복구.
- **검증 메모**: 팀원에게 통계가 비어 보이는 건 permission 규칙5(Task.createdBy 부재) 때문 — 버그 아님. createdBy 추가 시 해소(후속).
- **남은 것 / 다음 작업**: 단계 병목분석(§3.3, SLA 보류와 함께)·팀비교(팀모델)·외부캘린더(Phase3). 백엔드는 이팀장.

---

## 오늘 요약 (2026-06-01)
- 완료: **Sprint 17(활동량·실천 피드백 화면)**
- 화면 현황: 핵심 화면 사실상 전부 구현. 남은 건 외부캘린더(Phase3)·병목분석(SLA 보류 연동)·팀비교(팀모델)
- 백엔드(이팀장): 실제 인증·스케줄러·발송·완료이력 누적 등
- 새 결정사항/금지사항: 활동량 권한 필터(canViewTask) 적용 — 01_결정사항_누적.md 반영
