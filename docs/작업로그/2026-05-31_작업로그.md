# 2026-05-31 작업로그

## [작업 1] 프로젝트 워크스페이스 이동 + Sprint 11 plan 작성 (시각: 세션 시작)

- **무엇을**: (1) 클론 위치 `/tmp/crm-20260530` → `~/문서/crm-20260530`(영구 위치)로 이동, (2) 통합검색 Sprint 11 plan 작성
- **왜** (사용자 요청 추적): 사용자가 "이 레포부터 시작 합시다" + AskUserQuestion에서 워크스페이스 `~/문서/crm-20260530` + 다음 작업 "통합검색 구현" 선택. CLAUDE.md §3에 따라 1일+ 작업은 plan 먼저 작성 → 승인 → 구현.
- **어떻게**:
  - 워크스페이스: `mv /tmp/crm-20260530 ~/문서/crm-20260530` (git 상태 그대로 보존)
  - CLAUDE.md §0 필수 절차 완료: CLAUDE.md / 00_금지사항 / 01_결정사항 / 2026-05-30 작업로그 전부 읽음
  - Sprint plan 작성: `docs/sprints/11-통합검색.md` (목표 / 6개 시나리오 / 영향 파일 / 6단계 step→verify / 리스크 / 자기비판)
  - 작성 직후 head·wc·grep로 저장 검증 (OneDrive 동기화 금지사항 준수)
- **변경 파일 목록**: `docs/sprints/11-통합검색.md` (신규), `docs/작업로그/2026-05-31_작업로그.md` (이 파일, 신규)
- **이전 상태 → 이후 상태**: 클론 임시 위치(`/tmp`) + 통합검색 미계획 → 영구 위치 + Sprint 11 plan 승인대기
- **커밋 해시**: (승인 전이라 커밋 없음. 사용자 승인 후 plan + 작업로그 일괄 커밋 예정)
- **검증**: plan 48줄·승인란 명시 확인. 워크스페이스 이동 후 `ls ~/문서/crm-20260530` 정상.
- **남은 것 / 다음 작업**: 사용자가 Sprint 11 plan 검토 → 승인 시 구현 단계 1(타입+API) 진행. 승인 전 코드 작성 금지.

---

## [작업 2] Sprint 11 plan v2 — 권한 필터 반영

- **무엇을**: Sprint 11 plan에 "권한 필터링" 요구 반영 (사용자 추가 지시: "로그인한 상태에서 일부 권한이 들어간 사람들만 보여야 함")
- **왜** (사용자 요청 추적): 검색이 권한 우회 통로가 되면 §8(개인정보) 위반. 사용자가 명시적으로 권한 필터 요구.
- **어떻게**:
  - 타입 + mock 데이터 권한 모델 조사: `Customer.ownerId` / `Task.share(scope, sharedWith)` / `User.role(대표·팀장·팀원)` 확인. `User.teamId` 없음·`Task.ownerId` 없음 → 1차 한계로 명시.
  - plan v2 작성: 시나리오 7·8(권한 검증) 추가, 단계 1에 "mock currentUser + 권한 헬퍼" 신설, 영향 파일에 `api/auth.ts`·`utils/permission.ts` 추가, 권한 필터 규칙 표·한계·미해결 결정사항 4건 추가
  - "목 데이터 우선 + api/ 단일 통로" 결정사항 활용: Sprint 01(진짜 인증) 완성 시 mock currentUser만 교체하면 됨
- **변경 파일 목록**: `docs/sprints/11-통합검색.md` (Write 갱신, 48→108줄)
- **이전 상태 → 이후 상태**: plan v1(권한 미고려) → plan v2(권한 필터 포함, 사용자 결정 4건 명시)
- **커밋 해시**: (승인 전 — 미커밋)
- **검증**: 108줄·시나리오 14건(grep)·승인란 명시 확인
- **남은 것 / 다음 작업**: 사용자 결정 4건 + plan 승인. 승인 후 단계 1(currentUser+권한 헬퍼) 진행. 승인 전 코드 작성 금지.

---

## [작업 3] Sprint 11 단계 1 구현 — mock currentUser + 권한 필터 헬퍼

- **무엇을**: plan 단계 1 ("mock currentUser + 권한 헬퍼") 구현 + 단위 검증 + 커밋
- **왜** (사용자 요청 추적): plan v2 승인 (4가지 결정 권장안 답변) → 즉시 단계 1 진행
- **어떻게**:
  1. `frontend/src/mock/data.ts`: cust3 (ownerId=u2 김팀장) 1건 추가 — 권한 필터 검증용 (모든 기존 customer가 u3 소유라 시나리오 7 검증 불가했음)
  2. `frontend/src/api/auth.ts`(신규): `getCurrentUser()` mock + dev 전환 `setMockCurrentUserForDev()`. 디폴트 u3(이영업 팀원)
  3. `frontend/src/utils/permission.ts`(신규): `canViewCustomer/canViewTask/canViewPipeline` 순수 함수
     - 대표 → 전체 true
     - Customer: `ownerId === user.id`
     - Task: share.scope team OR sharedWith.includes OR customerId의 owner === user.id (Task ownerId 부재 한계)
     - Pipeline: customer 권한 상속
  - 순차 작성 + 매 단계 head/grep 검증 (OneDrive 금지사항 준수)
  - 빌드 검증: `npm install` 후 `npx tsc -b` EXIT 0 → `npm run build` 통과 (338.81KB)
  - 단위 검증: 임시 `_perm-verify.mts` + `node --experimental-strip-types` 실행 결과 5/5 통과
    · u3 → cust1(own): true ✓ / u3 → cust3(u2 own): **false** ✓ / 대표 → cust3: true ✓
    · u3 → t1(own cust): true ✓ / u3 → p1(own cust): true ✓
  - 임시 검증 파일 삭제 후 커밋
- **변경 파일 목록**: `frontend/src/mock/data.ts`, `frontend/src/api/auth.ts`(신규), `frontend/src/utils/permission.ts`(신규), `docs/sprints/11-통합검색.md`(이전 작업), `docs/기준/01_결정사항_누적.md`(이전 작업), `docs/작업로그/2026-05-31_작업로그.md`(이 파일)
- **이전 상태 → 이후 상태**: 권한 시스템 부재 → 검색에 쓸 헬퍼 인터페이스 완성 (단위검증 통과)
- **커밋 해시**: `1eaf559`
- **⚠️ 커밋 author 이슈**: 이 Mac의 git config(user.name/email)가 비어 fallback로 `이대규 <daegyulee@idaegyuui-Macmini-2.local>` 로 커밋됨. 이전 모든 커밋은 `신성민 <soung1031@gmail.com>`. 사용자 결정 필요 — (A) author 그대로 두기 (B) amend 후 신성민으로 통일 (C) git config 설정 후 다음 커밋부터 적용.
- **검증**: tsc -b EXIT 0, vite build 통과, 권한 헬퍼 단위 검증 5/5 통과
- **남은 것 / 다음 작업**: author 결정 후 단계 2 (타입 SearchHit/SearchResult + `api.searchAll(q)` 구현 — 권한 필터 적용).

---

## [작업 4] 레포 운영 방식 확정 + git config 설정 + 커밋 author 수정

- **무엇을**: (1) 두 레포 별도 운영 (A안) 사용자 확정, (2) git config --local 설정, (3) 1eaf559 커밋 amend
- **왜** (사용자 요청 추적): "고보험 깃이랑 신규 받아온 깃이랑 따로 운영이 되나?" → AskUserQuestion 답: A안 + 신성민 author로 amend
- **어떻게**:
  - 결정사항 누적에 3건 추가: 별도 레포 유지 / CRM 백엔드는 이팀장 / git config 신성민
  - `git config --local user.name "신성민"` + `git config --local user.email "soung1031@gmail.com"`
  - `git commit --amend --reset-author --no-edit` → 새 해시 7e24128
- **변경 파일 목록**: `docs/기준/01_결정사항_누적.md`, `docs/작업로그/2026-05-31_작업로그.md` (이 파일), `.git/config` (로컬 설정, 추적 안 됨)
- **이전 상태 → 이후 상태**: 커밋 author = fallback 이대규 / 레포 운영 방식 모호 → 신성민 통일 / A안 명시
- **커밋 해시**: `7e24128` (이전 1eaf559를 author 변경으로 대체)
- **검증**: `git log -1` 으로 author + 메시지 확인. config 적용은 --local이라 이 레포만 적용 (다른 디렉토리 무관).
- **남은 것 / 다음 작업**: 단계 2 (타입 SearchHit/SearchResult + api.searchAll 구현) 진행. **결정사항 누적 변경은 다음 커밋에서 포함.**

---

## [작업 5] Sprint 11 단계 2~4 통합 구현 — searchAll + 검색바 + 결과 페이지

- **무엇을**: plan 단계 2(타입+API) + 3(검색바) + 4(SearchPage+라우트) 한 단위로 구현
- **왜**: 이 셋은 서로 강하게 결합 (타입→API→화면). 한 커밋으로 묶어 회귀 단위 명확.
- **어떻게**:
  1. `types/index.ts`: `SearchHitKind`, `SearchHit`, `SearchResult` 타입 추가
  2. `api/index.ts`:
     - import에 `SearchHit`, `SearchResult`, `getCurrentUser`, `canViewCustomer/Task/Pipeline` 추가
     - `api.searchAll(q)` 신규 — currentUser 권한 필터 + substring(대소문자 무시) 매칭
     - 매칭 필드 1차(결정사항): 고객 이름·전화 / 할일 title / 파이프라인 차량번호·차대번호 + 고객 이름(간접)
     - 빈 검색어 → 즉시 빈 결과 반환
  3. `pages/SearchPage.tsx` + `SearchPage.css` 신규
     - URL `?q=` → `api.searchAll` → 3그룹 카드 표시
     - 빈 입력 안내 / 로딩 / 0건 안내(권한 힌트 포함)
     - 항목 클릭 → 해당 목록 페이지로 `?focus=id` 강조 이동
  4. `main.tsx`: `<Route path="search">` 추가 (AppShell children)
  5. `app/AppShell.tsx`: `SearchBar` 내부 컴포넌트 + PC 사이드바 상단 + 모바일 헤더 위치
     - HMR 금지사항 준수: import(useState, useNavigate, FormEvent) 먼저 추가 → 사용처 추가
     - 빈 입력 흔들림 피드백(시나리오 3)
  6. `app/AppShell.css`: `.search-bar*` 스타일 + 헤더 검색바는 모바일에서만 노출
- **변경 파일 목록**: `frontend/src/types/index.ts`, `frontend/src/api/index.ts`, `frontend/src/pages/SearchPage.tsx`(신규), `frontend/src/pages/SearchPage.css`(신규), `frontend/src/main.tsx`, `frontend/src/app/AppShell.tsx`, `frontend/src/app/AppShell.css`
- **이전 상태 → 이후 상태**: 검색 기능 부재 → 권한 필터 적용된 통합검색 동작 (코드 완성, UX 검증 남음)
- **커밋 해시**: `17834e6`
- **검증 (코드 레벨)**:
  - `npx tsc -b` EXIT 0
  - `npm run build` 통과 (344.12kB / gzip 106.49)
  - 권한 헬퍼는 [작업 3]에서 단위 검증 5/5 통과 → searchAll 내부 호출도 동일 동작 보장
- **검증 (UX·시나리오) — 사용자 dev preview 필요**:
  - 시나리오 1·2·5·6: "길" 검색 시 홍길동·관련 task 보임, 클릭 시 `?focus=id` 이동, "zxzx" 검색 시 0건 안내
  - 시나리오 3: 빈/공백 입력 시 검색바 흔들림, 페이지 이동 없음
  - 시나리오 4: 모바일(브라우저 좁히기)에서 헤더에 검색바 노출
  - **시나리오 7·8 (권한)**: u3로 "박타팀" 검색 → 0건 / `setMockCurrentUserForDev('u1')` 콘솔 호출 후 동일 검색 → cust3 노출
- **남은 것 / 다음 작업**:
  - dev preview 실행: `cd frontend && npm run dev` → http://localhost:5173 → 위 시나리오 확인
  - 사용자 검증 통과 시 작업로그에 통과 확정 + (선택) GitHub 푸시
  - Sprint 11 종료 후 미해결 항목(가중치, Cmd+K, 히스토리, 다중 단어 AND) → 후속 sprint

---

## [작업 6] Sprint 12 시작 — 고보험 서버에 CRM 통합 배포

- **무엇을**: 고보험 Cloud Run 컨테이너에 CRM 빌드를 `/crm` 패스로 합쳐 단일 서비스로 배포. Basic Auth 게이트로 사내 보호.
- **왜** (사용자 요청 추적): 사용자 "일단 고보험 서버에 붙여주세요" → AskUserQuestion → A안(/crm 정적 서빙) + Basic Auth 게이트
- **사전 경고 (Stop the line §7)**: CRM 인증 미완성(Sprint 01) 상태에서 외부 도메인 노출은 정보 노출 확대. → Basic Auth 게이트로 임시 보호. Sprint 01 완성 시 진짜 인증으로 교체.
- **단계 (예정)**:
  1. [CRM] `vite.config.ts` 에 `base: '/crm/'` + `createBrowserRouter` basename `/crm` 설정 → 서브패스 배포 대응
  2. [CRM] `npm run build` 통과 확인 → CRM origin/main 푸시 (고보험 Cloud Build 가 git clone 할 ref)
  3. [고보험] Dockerfile multi-stage에 CRM 빌드 단계 추가 (git clone → npm ci → vite build → dist/crm 복사)
  4. [고보험] server.js 에 Basic Auth 미들웨어 + `/crm/*` 정적 서빙 + SPA fallback
  5. [고보험] Cloud Run env 변수 안내: `CRM_BASIC_AUTH_USER`, `CRM_BASIC_AUTH_PASS` (사용자 직접 설정)
  6. [고보험] deploy → `https://goboheom.com/crm` 접근 시 Basic Auth 팝업 + 통과 후 CRM 화면 확인
  7. [양 레포] 작업로그 + 커밋
- **변경 파일 예정**:
  - CRM: `vite.config.ts`, `main.tsx` (basename)
  - 고보험: `Dockerfile`, `server.js`, 새 미들웨어 1개
- **남은 것 / 다음 작업**: 위 단계 순서대로. **Basic Auth ID/PW 는 사용자가 직접 Cloud Run env 에 설정**하므로 코드에 평문 없음.

---

## [작업 7] Sprint 12 단계 1·2 — CRM 서브패스 빌드 + 푸시

- **무엇을**: CRM 빌드 산출물을 `/crm/` 서브패스에서 정상 동작하도록 설정 + origin/main 푸시 (고보험 Cloud Build 가 참조)
- **왜**: 고보험 컨테이너의 `/crm/*` 에 정적 서빙하려면 자산 경로(`<script src=...>`)와 React Router basename 둘 다 prefix 맞아야 함
- **어떻게**:
  - `frontend/vite.config.ts`: `base: '/crm/'` 추가 → 빌드 시 모든 자산 URL 에 prefix
  - `frontend/src/main.tsx`: `createBrowserRouter(...,{ basename: '/crm' })` → SPA 라우팅 prefix
  - `npm run build` 통과 (344.14kB), `dist/index.html` 확인 → `<script src="/crm/assets/...">` 확정
- **변경 파일 목록**: `frontend/vite.config.ts`, `frontend/src/main.tsx`, `docs/작업로그/2026-05-31_작업로그.md`
- **이전 상태 → 이후 상태**: dev 만 가능(루트 /) → /crm/ prefix 빌드 가능 (서브배포 대응)
- **남은 것 / 다음 작업**: 커밋 + push. 그다음 고보험 측 Dockerfile/server.js 수정.
- **주의 (dev 영향)**: 로컬 dev 서버는 이제 http://localhost:5173/crm 로 접근해야 함 (base 적용). 기존 5173 dev 는 재시작 필요.

---

## [작업 8] Sprint 12 완료 — 라이브 검증 + 마무리

- **무엇을**: Sprint 12 (고보험 서버에 CRM 통합 배포 + Basic Auth 게이트) 라이브 검증 통과 + 작업로그·결정사항 누적 마무리
- **왜**: 단계 6(deploy + 검증) 완료. 사용자가 라이브 접속 확인 (`들어가는것 확인`).
- **어떻게**:
  - 고보험 [health_insure-main 레포] 변경 (해당 레포는 별도 작업로그 시스템 없음, 여기에 참조 기록):
    · `Dockerfile`: crm-build stage 신규 — `git clone --depth=1 ssm741031/crm-20260530.git` + `npm ci && npm run build` → `COPY --from=crm-build /crm-app/frontend/dist ./dist/crm`
    · `server.js`: Basic Auth 미들웨어(`crmBasicAuth`) + `/crm` 정적 + SPA fallback. Express 5 path-to-regexp 8 wildcard 호환성 이슈로 `/crm/*` → 정규식 `/^\/crm(?:\/.*)?$/` 로 교체
    · 고보험 커밋: `f970ad8` (초기 통합), `5ae2716` (wildcard fix). origin/main 푸시 완료
  - Cloud Run env 변수: `CRM_BASIC_AUTH_USER`, `CRM_BASIC_AUTH_PASS` 사용자 직접 등록 (값은 채팅에 미노출)
  - Deploy: `gcloud run deploy goboheom --source .` — 1차 시도(00053)는 wildcard 에러로 컨테이너 시작 실패 → 옛 image rollback → fix 후 2차 시도(00054) 성공
- **변경 파일 목록**: [고보험] `Dockerfile`, `server.js` / [CRM] `frontend/vite.config.ts`, `frontend/src/main.tsx`(basename), `docs/기준/01_결정사항_누적.md`, 이 작업로그
- **이전 상태 → 이후 상태**: CRM 로컬 dev only → https://goboheom.com/crm 라이브 (Basic Auth 보호)
- **커밋 해시 (고보험)**: f970ad8 (init), 5ae2716 (wildcard fix)
- **검증 (라이브)**:
  - Cloud Run revision: `goboheom-00054-5ts` (100% traffic)
  - `https://goboheom.com/` → 200 (고보험 그대로) ✓
  - `https://goboheom.com/crm/` → **401** + `WWW-Authenticate: Basic realm="CRM"` ✓
  - `https://goboheom.com/crm/index.html` 직접 → **401** (dist/crm 직접 우회 차단) ✓
  - 사용자 브라우저 접속 + Basic Auth 입력 → CRM 메인 진입 확인 ✓
- **사용자 질문 답변 (작업 종료 시점 기준)**:
  - Q. 계정별 고객관리 되나? → **안 됨**. mock currentUser u3 고정 + Basic Auth 공유 1계정 + Sprint 01 미완. 지금은 외부 차단된 사내 데모 수준.
- **남은 것 / 다음 작업**:
  - Sprint 11 UX 시나리오 7·8 라이브 직접 검증 (콘솔 dev 토글 미사용 시 별도 토글 UI 또는 dev URL 필요 — 후속)
  - Sprint 01 (이팀장 영역): 백엔드·DB·진짜 인증 → 완성 시 `api/auth.ts` mock 교체로 자동 적용
  - 로컬 CRM dev server(pid 72169, task b6q1vstl1) 종료 완료
- **백그라운드 정리**:
  - CRM Vite dev (b6q1vstl1) → TaskStop 완료
  - 사용자 본인의 `~/eatalone` Next.js dev (pid 6836)는 무관 — 그대로 유지

---

## [작업 9] Sprint 13 시작 — 프론트 인증 골격 (mock 기반)

- **무엇을**: Sprint 13 plan 작성 + 4가지 결정 확정 + 구현 단계 1~6 진행
- **왜** (사용자 요청 추적): "고객관리 개인화 어떻게 하면 되는지 가이드주세요" → 가이드 + plan v1 → 4결정 권장안 답변 → 즉시 구현 시작
- **단계별 (plan v1 기준 7단계)**:
  1. api/auth.ts 갱신 (login/logout/restoreSession + getCurrentUser 통합)
  2. contexts/AuthContext + hooks/useAuth
  3. components/RequireAuth (라우트 가드)
  4. pages/LoginPage + .css
  5. main.tsx (AuthProvider, /login, RequireAuth 래핑)
  6. AppShell.tsx (헤더에 사용자명+로그아웃)
  7. 빌드+검증+커밋
- **결정사항 5건** (01_결정사항_누적.md 에 누적): mock pw "1234" 통일 / 로그인 후 /tasks / 로그아웃 후 /login / dev 토글 유지(DEV 가드) / localStorage 저장(백엔드 시 재결정)
- **변경 파일 예정**:
  - 신규: AuthContext.tsx, useAuth.ts, RequireAuth.tsx, LoginPage.tsx, LoginPage.css
  - 수정: api/auth.ts, main.tsx, app/AppShell.tsx
- **남은 것**: 코드 작성 + tsc/build + 커밋 + push. 사용자 시나리오 검증은 고보험 재배포 후 라이브에서.

---

## [작업 10] Sprint 13 단계 1~6 통합 구현 — 인증 골격 완성

- **무엇을**: plan 단계 1(api/auth.ts) + 2(Context/훅) + 3(가드) + 4(LoginPage) + 5(main.tsx) + 6(AppShell 헤더) 한 묶음 구현
- **왜**: 서로 강하게 결합 (api ↔ Context ↔ 가드 ↔ 라우터 ↔ 헤더). 한 커밋으로 회귀 단위 명확.
- **어떻게**:
  1. `api/auth.ts` 전면 재작성: `login(id,pw)` / `logout()` / `restoreSession()` / `getCurrentUser()` (User|null) / `setMockCurrentUserForDev` (DEV 가드)
     - 메모리 + localStorage(`crm.session.v1`) 세션. mock pw "1234" 통일
  2. `api/index.ts` `searchAll`: getCurrentUser null 처리 (비로그인 → 빈 결과, RequireAuth 외 안전 가드)
  3. `contexts/AuthContext.tsx`(신규): React Context + AuthProvider, 마운트 시 restoreSession
  4. `hooks/useAuth.ts`(신규): Context wrapper, Provider 밖 호출 시 throw
  5. `components/RequireAuth.tsx`(신규): loading 중 빈 화면 / 비로그인 → /login (state.from 보존)
  6. `pages/LoginPage.tsx` + `LoginPage.css`(신규): mock 폼 + DEV 안내 + dev 계정 힌트
  7. `main.tsx`: AuthProvider 감싸기 + `/login` 별도 라우트 + RequireAuth 로 AppShell 전체 보호 (Sprint 12 basename `/crm` 유지)
  8. `app/AppShell.tsx`: UserBadge 컴포넌트(사용자명+role+로그아웃) 헤더 우측
  9. `app/AppShell.css`: `.user-badge*` 스타일 + 모바일 role 숨김
- **변경 파일 목록**: `frontend/src/api/auth.ts`(전면 재작성), `frontend/src/api/index.ts`(null 안전), `frontend/src/contexts/AuthContext.tsx`(신규), `frontend/src/hooks/useAuth.ts`(신규), `frontend/src/components/RequireAuth.tsx`(신규), `frontend/src/pages/LoginPage.tsx`(신규), `frontend/src/pages/LoginPage.css`(신규), `frontend/src/main.tsx`, `frontend/src/app/AppShell.tsx`, `frontend/src/app/AppShell.css`
- **이전 상태 → 이후 상태**: getCurrentUser mock u3 고정 / 비로그인 보호 X / 로그인 화면 X → 인증 골격 완성 (mock 로그인 → 라우트 가드 → 사용자별 currentUser → Sprint 11 권한 헬퍼 자동 작동)
- **커밋 해시**: (커밋 예정)
- **검증 (코드 레벨)**:
  - `npx tsc -b` EXIT 0
  - `npm run build` 통과 (348.06kB / gzip 107.66, 3KB 증가)
- **검증 (UX·시나리오) — 사용자 라이브 검증 필요**:
  - 시나리오 1·2·3·4·5·6·7 (로그인/리다이렉트/유지)
  - 시나리오 8·9 (권한 — sales1로 로그인 시 cust3 안 보임 / boss 로그인 시 보임)
- **남은 것 / 다음 작업**:
  - CRM origin/main push
  - 고보험 Cloud Run 재배포 (Dockerfile 의 CRM_GIT_REF=main 이라 main HEAD 반영)
  - 사용자 라이브 검증 (https://goboheom.com/crm → /login 폼 → 로그인 → CRM 메인)

---

## [작업 11] 이팀장 인계 문서 — 백엔드 API 계약 작성

- **무엇을**: 이팀장이 Sprint 01(인증·권한 백엔드) 시작할 수 있도록 **API 계약 + 결정 체크리스트** 문서 작성
- **왜** (사용자 요청 추적): 사용자 "지금 데이터 개인화는 되나요?" → 읽기 권한 분리는 됨/쓰기 격리는 백엔드 필요 → "이팀장에게 백엔드 Sprint 01 시작 요청 준비를 제가 돕기" 선택
- **어떻게**:
  - `docs/sprints/01-백엔드-API계약.md` 신규 (221줄, 13개 섹션)
  - 섹션: 인증 API 3건 (login/logout/me) / 데이터 모델 (User SQL) / CRUD API / 권한 미들웨어 (Sprint 11 헬퍼와 일치) / 에러 표준 / 환경 / 마이그레이션 / 결정 체크리스트 7건 / 권장 순서 10단계 / 보안 의무 / 소통 포인트
  - 핵심: 프론트(Sprint 11+13) 가정과 일치하게 만들면 작업자가 fetch 교체만으로 화면 변경 0 으로 전환 가능
  - 결정 필요 (이팀장): 프레임워크(FastAPI/Express), 인증방식(세션/JWT), 배포구조(통합/별도), DB, User ID 체계, teamId, password 정책
  - 권장안 명시: 인증=세션 쿠키(httpOnly), 배포=고보험 컨테이너 통합 (단순)
- **변경 파일 목록**: `docs/sprints/01-백엔드-API계약.md` (신규), 이 작업로그
- **이전 상태 → 이후 상태**: Sprint 01 백엔드 작업 가이드 부재 → 이팀장이 즉시 시작 가능한 API 계약 + 결정 체크리스트
- **커밋 해시**: (커밋 예정)
- **검증**: 13개 섹션·221줄·결정 체크리스트 7건·권장 순서 10단계 확인
- **남은 것 / 다음 작업**:
  - 사용자가 이팀장에게 이 문서 전달
  - 이팀장 결정사항 답변 후 작업자가 결정사항 누적에 반영
  - 백엔드 진행 따라 작업자가 mock → fetch 마이그레이션 (단계별)

---

## [작업 12] Sprint 14 — 알람 Phase 1 구현 (포그라운드)

- **무엇을**: plan 단계 1~8 통합 구현 — 클라이언트 알람 스케줄러 + Notification API + Toast fallback
- **왜** (사용자 요청 추적): "알람 기능을 PC·모바일 모두" → Phase 분할 추천 → Sprint 14(Phase1) 즉시 시작 선택
- **단계**:
  1. utils/reminders.ts — Task → upcoming reminder times
  2. NotificationContext + useNotification 훅 + 스케줄러
  3. NotificationPermissionBanner
  4. Toast + ToastContainer
  5. AppShell + main.tsx 통합
  6. tsc+build+커밋
- **결정사항 6건** (01_결정사항_누적.md 반영): 채널·이동경로·토스트위치·배너위치·자동요청·본문양식
- **남은 것**: 코드 작성 + 검증 + 커밋 + 고보험 재배포

---

## [작업 13] Sprint 14 단계 1~6 통합 구현 — 알람 Phase 1

- **무엇을**: plan 단계 1~8 한 묶음 구현 (단계 7 dev preview 는 사용자 라이브 검증, 단계 8 빌드 통과 확인)
- **어떻게**:
  1. `utils/reminders.ts`(신규): `computeReminderTimes` / `computeAllUpcoming` / `reminderKey` / `buildReminderText` — Task → 미래 알람 시각 + 본문 빌드
  2. `contexts/NotificationContext.tsx`(신규) + `hooks/useNotification.ts`(신규):
     - `permission` 상태 (default/granted/denied/unsupported)
     - `requestPermission()` 메서드
     - 토스트 큐 + 자동 5초 닫기
     - 스케줄러: `api.getTasks()` → `computeAllUpcoming` → setTimeout 등록. 발사 시 dedup (Set), 라우트/마운트 cleanup
     - 권한 granted: `new Notification(title, {body, tag})` + onclick → /tasks?focus=id navigate / 권한 없거나 unsupported: 토스트 fallback
  3. `components/NotificationPermissionBanner.tsx`(신규) + `.css`: 권한 default 일 때만 표시. 허용/닫기 버튼
  4. `components/ToastContainer.tsx`(신규) + `.css`: 우측 상단 fixed 스택, [보기]/[닫기] 버튼, 클릭 시 navigate
  5. `app/AppShell.tsx`: `ShellInner` 추출 + `AppShell` 이 `NotificationProvider` 로 감싸기 (useNavigate 가 Router 안에 있어야 동작), 배너+ToastContainer 마운트
  - HMR/OneDrive 금지사항 준수 (import 먼저, 순차 작성)
- **변경 파일 목록**: `frontend/src/utils/reminders.ts`(신규), `frontend/src/contexts/NotificationContext.tsx`(신규), `frontend/src/hooks/useNotification.ts`(신규), `frontend/src/components/NotificationPermissionBanner.tsx`(신규), `frontend/src/components/NotificationPermissionBanner.css`(신규), `frontend/src/components/ToastContainer.tsx`(신규), `frontend/src/components/ToastContainer.css`(신규), `frontend/src/app/AppShell.tsx`(수정)
- **이전 상태 → 이후 상태**: 알람 기능 0 → Phase 1 완성 (포그라운드 OS 알림 + 토스트 fallback + 권한 배너 + 스케줄러)
- **커밋 해시**: (커밋 예정)
- **검증 (코드)**:
  - `npx tsc -b` EXIT 0
  - `npm run build` 통과 (352.55kB / gzip 109.01)
- **검증 (UX) — 사용자 라이브 검증**:
  - 시나리오 1~9 (배너/권한/OS 알림/클릭이동/토스트/dedup/cleanup/동작분기/재감지)
  - 검증 팁: TasksPage 에서 `endTime` 을 2-3분 후로 설정한 task 추가 + reminders=[{minutesBefore: 1}] → 1-2분 후 알림 발사 확인
- **남은 것 / 다음 작업**:
  - CRM push + 고보험 재배포
  - 사용자 라이브 검증
  - Phase 2 (PWA + Service Worker + Push 백엔드) → Sprint 15/16 — 백엔드 협업

---

## [작업 14] 모바일 로그인 버그 수정 — LoginPage navigate 안티패턴

- **무엇을**: LoginPage 함수 본체에서 `navigate()` 호출 → `useEffect` 로 이동
- **왜** (사용자 보고): "모바일에선 sales1/1234 로그인이 안 된다"
- **원인 추정**: 함수 본체 내 navigate 호출은 React 안티패턴 (StrictMode + 모바일에서 race/리렌더 무한 가능). 로그인 성공 → user 상태 업데이트 → 리렌더 → 함수 본체 navigate 호출 → 새 라우트로 가기 전 추가 렌더가 꼬일 수 있음.
- **변경**: `frontend/src/pages/LoginPage.tsx` — `if (user) { navigate(); return null }` → `useEffect(() => { if (user) navigate() }, [user, from, navigate])`
- **검증 (코드)**: tsc EXIT 0, build EXIT 0
- **남은 것**: 사용자 모바일 재검증 — 그래도 안 되면 증상(폼 자체 X? 입력 후 무반응? 에러?) 알려달라 요청. 한글 IME 합성 이슈 가능성도 있음.

**추가 안내 (CRM 버튼 모바일)**: 모바일 헤더가 아닌 **하단 탭바 가장 우측 ☰ 메뉴** 클릭 → 더보기 시트 → 로그인 시 "사내CRM ↗" 보임. 헤더에는 모바일에서 CRM 버튼 자리 없음 (의도된 — 더보기 메뉴로 통합).

---

## [작업 15] 승인대기 스프린트 3개 사후 승인 정리 (문서 일관성)

- **무엇을**: Sprint 11(통합검색)·13(인증 골격)·14(알람 Phase1) 문서의 상태/완료기준/사용자결정/승인란을 실제 구현 상태에 맞게 정정
- **왜** (사용자 요청 추적): 핸드폰 원격 접속 후 "작업 어디까지 했는지 확인해서 리스트 알려줘" → 현황 점검 결과 세 스프린트가 **코드 구현·커밋·라이브 검증은 끝났는데 문서 승인란만 ☐로 남아** CLAUDE.md 3장(승인 전 구현 금지)과 모순. 사용자가 "승인대기 3개 정리" → "사후 승인으로 정리" 선택.
- **어떻게** (Sprint 04 정정 선례와 동일 방식 — 승인 날조 아닌 사후 정정 명시):
  - 상태: `승인대기` → `완료 (커밋 해시 / 2026-05-31 사후 승인 정리)`
  - 완료기준 체크박스: 실제 검증된 시나리오 `[ ]`→`[x]`
  - "사용자 결정 필요" → "확정 결과 (구현 반영됨)" — 전부 권장 A안대로 구현된 상태를 확정 기록
  - 승인란: `☐` → `☑ 사후 승인 정리 (2026-05-31)` + 커밋 근거 명시
- **변경 파일 목록**:
  - `docs/sprints/11-통합검색.md`, `docs/sprints/13-프론트-인증-골격.md`, `docs/sprints/14-알람-Phase1-포그라운드.md`
- **이전 상태 → 이후 상태**: 구현 완료됐으나 문서상 승인대기(모순) → 문서가 실제 상태와 일치(완료·승인 정리)
- **관련 커밋(기능, 이미 존재)**: 검색 `7e24128`·`b77a9c9` / 인증 `4b5fd07`(버그수정 `5d3fa7a`) / 알람 `14b388d`
- **커밋 해시**: (이 문서 정리 커밋 — 아래)
- **검증**: 3개 문서 모두 `grep "☑ 사후 승인"` 1건씩 확인, 상태줄 `완료`로 변경 확인
- **남은 것 / 다음 작업**: 화면 이월 작업 — SLA 기준시간 편집 화면+병목분석 / 안내기록·수신동의 UI / 일간 캘린더 양끝 리사이즈·목록순서 드래그. 백엔드(스케줄러·실제 인증·푸시)는 이팀장.

---

## [작업 16] 안내 기록 + 수신동의 입력 UI (Sprint 15)

- **무엇을**: 두 가지 수동 기록 UI 구현 — (A)고객 수신동의 편집 (B)청약 단계별 안내 기록
- **왜** (사용자 요청 추적): 핸드폰 원격 접속 후 "안내 기록/수신동의 입력 UI 진행하자". 둘 다 Phase 3 자동발송 자격판단 근거(계획서 §2.4-b·c). plan(Sprint 15)→승인("진행하자"+결정 3건)→구현.
- **사용자 결정**: 수신동의=고객 클릭 모달 / 안내기록=단계 패널+별도 모아보기 둘 다 / 수단=문자·카톡·전화
- **어떻게**:
  - 타입: `NoticeChannel`("문자"|"카톡"|"전화"), `NoticeLog{id,pipelineId,stageNo,channel,memo,sentAt,createdBy}` 신규
  - api: `updateConsent`, `getNoticeLogs(pipelineId?)`, `addNoticeLog`, `updateNoticeLog`, `deleteNoticeLog` (메모리 목). getCustomers를 mutable customers 반환으로 변경. 안내 일시(`new Date().toISOString()`)·작성자(`getCurrentUser().id`) 시스템 자동
  - `components/ConsentForm.tsx`(신규): 동의/거부/미확인 세그먼트 + 동의일. '동의' 시 동의일 없으면 오늘 자동
  - `pages/CustomersPage.tsx`: 카드 클릭→ConsentForm 모달, 수신동의 색 배지(동의=초록/거부=빨강/미확인=회색)
  - `components/NoticeLogPanel.tsx`(신규, 재사용): 단계의 안내 기록 추가폼(수단+메모)+목록+삭제. PipelinePage 각 단계에 마운트, 단계엔 "📨 안내 N" 배지
  - `pages/NoticeLogsPage.tsx`(신규): 전체 안내 기록 모아보기(고객·상품·단계 맥락+일시+작성자), 메모 인라인 수정·삭제. nav/라우트(/notices) 추가
  - `.btn--sm` 전역화(index.css), mock 안내기록 샘플 2건
- **변경 파일 목록**: `types/index.ts`, `api/index.ts`, `mock/data.ts`, `components/ConsentForm.tsx`·`.css`(신규), `components/NoticeLogPanel.tsx`·`.css`(신규), `pages/CustomersPage.tsx`, `pages/NoticeLogsPage.tsx`(신규), `pages/PipelinePage.tsx`·`.css`, `pages/page.css`, `app/nav.ts`, `main.tsx`, `index.css`
- **이전 상태 → 이후 상태**: 수신동의 읽기전용·안내기록 없음 → 수신동의 편집 + 안내기록 추가/조회/수정/삭제(단계+모아보기)
- **커밋 해시**: `c9c6b3a`
- **검증** (미리보기, sales1 로그인):
  - 수신동의: 김샘플 문자 미확인→동의 저장, 배지 반영, 동의일 자동
  - 안내기록 추가: 홍길동 1단계 "전화/DB 수집 확인 전화 드림" → 일시 13:42·작성자 이영업 자동, 단계 "📨 안내 1" 배지
  - 모아보기: 3건 표시(고객·상품·단계 맥락), 메모 인라인 수정("(수정됨)") 반영
  - tsc EXIT 0, 프로덕션 빌드 통과, 콘솔 에러 0
- **남은 것 / 다음 작업**: 일간 캘린더 양끝 리사이즈·목록순서 드래그 / 외부캘린더(Phase3). 백엔드(이팀장): 실제 인증·스케줄러·발송. SLA 기준시간 편집은 보류(임시운영 시).

---

## 오늘 요약 (2026-05-31)
- 완료: Sprint 11(통합검색)·13(인증 골격)·14(알람 Phase1) 구현 + 모바일 로그인 버그 수정 + 승인대기 3개 사후 승인 정리 + **Sprint 15(안내기록·수신동의)**
- 화면 구현 현황: 완료 화면 — 할일·반복알람·자동재생성·캘린더4종·드래그·습관·카테고리·고객(+수신동의)·파이프라인(+안내기록)·통합검색·로그인·알람·안내기록 모아보기
- 미완료/이월(화면): 일간 리사이즈·목록순서 드래그, 외부캘린더(Phase3). SLA 기준시간 편집=보류(임시운영 시)
- 백엔드(이팀장): 실제 인증, 스케줄러, 빼앗긴 고객 처리, 반복 자동전개, 실제 푸시·발송
